apiVersion: v1 
kind: Pod
metadata:
  name: limits
spec:
  containers:
  - name: nginx
    image: nginx
    resources: 
      requets:
        cpu: "100m"
        memory: "68Mi"
      limts:
        cpu: "120m"
        memory: "100Mi"
    env:
      project: expense

