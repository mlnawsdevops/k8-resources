apiVersion: v1 
kind: Pod
metadata:
  name: multicontainer
  namespace: expense
spec:
  containers:
  - name: nginx
    image: nginx
  - name: almalinux
    image: almalinux:9
    command: ["Sleep 100"]